#!/bin/sh

if [ -d "<%= @basic[:home] %>/vertice" ]; then
  rm -r <%= @basic[:home] %>/<%= @package[:pacakge] %>
fi

if [ -d "<%= @basic[:libhome] %>/vertice" ]; then
  rm -r <%= @basic[:libhome] %>/<%= @package[:pacakge] %>
fi

if [ -d "<%= @basic[:loghome] %>/<%= @package[:package] %>" ]; then
  rm -r <%= @basic[:loghome] %>/<%= @package[:package] %>
fi
